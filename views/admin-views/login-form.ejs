<!doctype html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport"
        content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Admin panel | Login</title>
  <style>
      * {
          box-sizing: border-box;
      }
      body {
          display: flex;
          align-items: center;
          justify-content: center;
      }
      input {
          display: block;
          width: 100%;
          height: 2rem;
          margin-bottom: 1rem;
          padding: 3px;
          border-radius: 5px;
          outline: none;
      }
      .container {
          margin-top: 30vh;
          padding: 3rem;
          border-radius: 15px;
          background-color: #316419;
          text-align: center;
          font-family: Arial, serif;
          color: #ffffff;
      }
      .title {
          margin-top: 0;
      }
      .button {
          display: inline-block;
          padding: 11px;
          border: none;
          border-radius: 5px;
          text-align: center;
          transition: all 0.3s ease;
          background-color: #f3c650;
          color: #535353;
          font-weight: bold;
          font-size: 0.8rem;
          text-transform: uppercase;
          text-decoration: none;
          cursor: pointer;
      }
      .button:hover {
          transform: scale(1.05);
      }
      .back-link {
        margin-right: 1rem;
      }
  </style>
</head>
<body>
  <div class="container">
    <h3 class="title">Вход в панель администратора</h3>
    <p>Введите логин и пароль:</p>
    <form class="form" method="post" action="/login">
      <input name="admin" type="text" placeholder="Имя администратора" required>
      <input name="password" type="password" placeholder="Пароль" required>
      <a class="button back-link" href="/">На сайт</a>
      <button class="button" type="submit">Войти</button>
    </form>
  </div>
</body>
</html>
