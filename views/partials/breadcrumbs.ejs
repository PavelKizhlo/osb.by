<div class="breadcrumbs">
    <div class="container">
        <a class="breadcrumbs__link" href="/">Главная</a>
        <% crumbs.forEach(crumb => { %>
           <a class="breadcrumbs__link" href="<%= crumb.link %>"><%= crumb.name %></a>
        <% }); %>
    </div>
</div>
